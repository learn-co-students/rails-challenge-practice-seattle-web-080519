<h1>View all Companies</h1>
<% @companies.each do |company| %>
  <%= company.name %>   <%= link_to "View this Company", company_path(company) %> <br>
<% end %>

<%= link_to "Add a new Company", new_company_path %>